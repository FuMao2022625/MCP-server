整体方案说明

1. 搭建 MCP Server（stdio 模式）：提供 MySQL 查询工具，支持执行安全 SQL、查询表结构、查询数据

2. MCP Client 侧基于 Python，对接 DeepSeek API，让大模型自主调用 MCP MySQL 工具，完成自然语言转 SQL → 查询数据库 → 数据分析总结

3. 做 SQL 安全限制：仅允许 SELECT，禁止 DROP/ALTER/INSERT/DELETE，防止删库风险

4. 依赖：mcp 官方库、mysql-connector-python、openai SDK（DeepSeek 兼容 OpenAI 接口格式）

1. 安装依赖
pip install mcp mysql-connector-python python-dotenv openai
2. 环境变量 .env
# DeepSeek 配置
DEEPSEEK_API_KEY=你的DeepSeek密钥
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat

# MySQL 配置
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=你的数据库密码
MYSQL_DATABASE=test_db
3. MCP Server 服务：mysql_mcp_server.py

提供三个安全工具：查询表列表、查询表字段结构、执行只读SQL查询
from mcp.server import Server
from mcp.types import Tool, TextContent
import mysql.connector
from mysql.connector import Error
import os
from dotenv import load_dotenv

load_dotenv()
app = Server("mysql-mcp-server")

# MySQL 连接工具
def get_mysql_conn():
    try:
        conn = mysql.connector.connect(
            host=os.getenv("MYSQL_HOST"),
            port=int(os.getenv("MYSQL_PORT")),
            user=os.getenv("MYSQL_USER"),
            password=os.getenv("MYSQL_PASSWORD"),
            database=os.getenv("MYSQL_DATABASE")
        )
        return conn
    except Error as e:
        return None, str(e)

# 安全校验：只允许查询语句
def is_safe_sql(sql: str) -> bool:
    sql_upper = sql.strip().upper()
    forbidden = ["INSERT", "DELETE", "UPDATE", "DROP", "ALTER", "TRUNCATE", "CREATE"]
    if not sql_upper.startswith("SELECT"):
        return False
    for keyword in forbidden:
        if keyword in sql_upper:
            return False
    return True

@app.tool()
async def list_all_tables() -> str:
    """
    获取数据库内所有数据表名称列表
    """
    conn, err = get_mysql_conn()
    if err:
        return f"数据库连接失败：{err}"
    cursor = conn.cursor()
    cursor.execute("SHOW TABLES;")
    tables = [item[0] for item in cursor.fetchall()]
    cursor.close()
    conn.close()
    return f"数据库表列表：{tables}"

@app.tool()
async def get_table_schema(table_name: str) -> str:
    """
    查询指定数据表的字段结构
    Args:
        table_name: 数据表名称
    """
    conn, err = get_mysql_conn()
    if err:
        return f"数据库连接失败：{err}"
    cursor = conn.cursor()
    try:
        cursor.execute(f"DESC {table_name};")
        schema = cursor.fetchall()
        res = f"表 {table_name} 结构(字段名|类型|是否为空|主键|默认值|备注)：\n{schema}"
    except Exception as e:
        res = f"查询表结构失败：{str(e)}"
    cursor.close()
    conn.close()
    return res

@app.tool()
async def execute_select_sql(sql: str) -> str:
    """
    执行只读SELECT查询SQL语句，禁止增删改操作
    Args:
        sql: 纯SELECT查询语句
    """
    if not is_safe_sql(sql):
        return "SQL不安全，仅支持SELECT查询，禁止增删改删表等操作"
    conn, err = get_mysql_conn()
    if err:
        return f"数据库连接失败：{err}"
    cursor = conn.cursor()
    try:
        cursor.execute(sql)
        rows = cursor.fetchall()
        cols = [col[0] for col in cursor.description]
        result = {"columns": cols, "data": rows}
    except Exception as e:
        result = f"SQL执行异常：{str(e)}"
    cursor.close()
    conn.close()
    return str(result)

async def main():
    import mcp
    async with mcp.server.stdio.stdio_server() as (read_stream, write_stream):
        await app.run(
            read_stream,
            write_stream,
            app.create_initialization_options()
        )

if __name__ == "__main__":
    import asyncio
    asyncio.run(main())
4. MCP Client：对接 DeepSeek 做数据分析 client_deepseek_mcp.py

客户端拉起上面的 MCP 子进程，自动加载 MySQL 工具，交给 DeepSeek 自主决策：查表结构 → 生成SQL → 查询数据 → 分析结果
import os
import asyncio
from dotenv import load_dotenv
from openai import AsyncOpenAI
from mcp.client.stdio import stdio_client
from mcp.client.session import ClientSession

load_dotenv()

# 初始化 DeepSeek 客户端（兼容 OpenAI SDK）
llm_client = AsyncOpenAI(
    api_key=os.getenv("DEEPSEEK_API_KEY"),
    base_url=os.getenv("DEEPSEEK_BASE_URL")
)
MODEL_NAME = os.getenv("DEEPSEEK_MODEL")

# 将 MCP 工具转换为 OpenAI Function Call 格式
def mcp_to_function(tool):
    return {
        "type": "function",
        "function": {
            "name": tool.name,
            "description": tool.description,
            "parameters": tool.inputSchema
        }
    }

async def agent_loop(session: ClientSession, user_query: str):
    # 获取MCP全部可用工具
    tools_list = await session.list_tools()
    functions = [mcp_to_function(t) for t in tools_list.tools]
    messages = [
        {
            "role": "system",
            "content": """你是数据库数据分析智能助手，可以调用MySQL工具完成分析任务。
执行流程：
1. 不清楚表结构先调用 list_all_tables 查看所有表
2. 确定目标表后调用 get_table_schema 获取字段结构
3. 根据需求生成安全SELECT语句调用 execute_select_sql 查询数据
4. 拿到数据后做数据分析、汇总、趋势解读，禁止编造数据
仅使用工具返回真实数据回答用户问题。"""
        },
        {"role": "user", "content": user_query}
    ]

    max_round = 5
    for _ in range(max_round):
        resp = await llm_client.chat.completions.create(
            model=MODEL_NAME,
            messages=messages,
            tools=functions,
            tool_choice="auto"
        )
        msg = resp.choices[0].message
        if not msg.tool_calls:
            # 无工具调用，直接返回分析结果
            return msg.content

        # 执行MCP工具调用
        for tool_call in msg.tool_calls:
            func_name = tool_call.function.name
            func_args = eval(tool_call.function.arguments)
            tool_res = await session.call_tool(func_name, arguments=func_args)
            # 存入上下文
            messages.append(msg.model_dump())
            messages.append({
                "role": "tool",
                "tool_call_id": tool_call.id,
                "content": str(tool_res.content)
            })
    return "达到最大执行轮数，任务终止"

async def main():
    # 启动本地MCP Server子进程
    cmd = ["python", "mysql_mcp_server.py"]
    async with stdio_client(cmd) as (read, write):
        async with ClientSession(read, write) as session:
            await session.initialize()
            # 自定义你的数据库分析问题
            user_question = "帮我查看数据库里有哪些表，查看订单表结构，并统计订单总金额"
            result = await agent_loop(session, user_question)
            print("=====DeepSeek数据分析结果=====\n", result)

if __name__ == "__main__":
    asyncio.run(main())
5. 运行前置准备

1. 本地启动 MySQL，创建数据库 test_db，准备测试业务表（例如 orders 订单表）

2. 修改 .env 内 MySQL 账号密码、DeepSeek Key

3. 执行启动命令
python client_deepseek_mcp.py
6. 执行流程拆解

1. Client 拉起 MySQL-MCP 服务进程，通过 stdio 建立 MCP 通信

2. Client 获取 MCP 提供的 3 个 MySQL 工具，转换为 DeepSeek 可识别的 Function Call

3. DeepSeek 根据自然语言分析任务，自主决策调用顺序：
查看全表列表 → 获取目标表结构 → 生成 SELECT SQL 查询数据

4. MCP 服务执行安全 SQL，返回数据给大模型

5. DeepSeek 基于真实数据库数据完成汇总、统计、文字化分析输出

7. 安全优化拓展

1. 增加 SQL 白名单、字段权限隔离，限制只能访问指定数据表

2. 增加查询行数上限，防止超大结果集拖垮数据库

3. MCP 增加调用日志，记录所有执行的 SQL 用于审计

4. 支持远程 HTTP-SSE 模式 MCP，脱离本地 stdio，分布式调用

8. 可替换测试问题示例
1. 查看数据库所有表，找出用户相关表并查看结构
2. 查询2025年所有有效订单的总数量和总交易额并做简单分析
3. 统计每个用户的下单频次，给出简要用户分层分析